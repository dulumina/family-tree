{
  "name": "family-tree-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/prepare-client.js && nodemon src/index.js",
    "start": "node scripts/prepare-client.js && node src/index.js",
    "migrate": "node src/db/migrate.js"

  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "nodemon": "^3.1.14"
  }
}
